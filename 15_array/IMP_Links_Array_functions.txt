


1) https://www.php.net/manual/en/function.array-values.php

2) https://www.codecademy.com/resources/docs/php/arrays



$cleanedWaitingList = [];
foreach ($waitingList as $value) {
    if($value === 'Dawn White' || $value === 'Charlie Davis'){
        $cleanedWaitingList[] = $value
    }
}
$cleanedWaitingList = sort($cleanedWaitingList);
$selectedParticipants = array_chunk($cleanedWaitingList,5);


